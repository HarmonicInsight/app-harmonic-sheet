<UserControl x:Class="HarmonicSheet.Views.SpreadsheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <!-- ツールバー -->
            <RowDefinition Height="Auto"/>
            <!-- 計算ボタンバー -->
            <RowDefinition Height="Auto"/>
            <!-- コマンド入力エリア -->
            <RowDefinition Height="Auto"/>
            <!-- スプレッドシート本体 -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ========================================== -->
        <!-- ツールバー: 大きなボタン -->
        <!-- ========================================== -->
        <Border Grid.Row="0"
                Background="{StaticResource SurfaceBrush}"
                Padding="8,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Style="{StaticResource ActionButtonStyle}"
                            MinWidth="150"
                            MinHeight="60"
                            Padding="20,12"
                            Click="OnRecentClick"
                            ToolTip="最近使ったファイル">
                        <TextBlock Text="前回使用" FontSize="24" FontWeight="Bold"/>
                    </Button>
                    <Button Style="{StaticResource ActionButtonStyle}"
                            MinWidth="150"
                            MinHeight="60"
                            Padding="20,12"
                            Click="OnNewClick">
                        <TextBlock Text="新規" FontSize="24" FontWeight="Bold"/>
                    </Button>
                    <Button Style="{StaticResource ActionButtonStyle}"
                            MinWidth="150"
                            MinHeight="60"
                            Padding="20,12"
                            Click="OnOpenClick">
                        <TextBlock Text="開く" FontSize="24" FontWeight="Bold"/>
                    </Button>
                    <Button Style="{StaticResource ActionButtonStyle}"
                            MinWidth="150"
                            MinHeight="60"
                            Padding="20,12"
                            Click="OnSaveClick">
                        <TextBlock Text="保存" FontSize="24" FontWeight="Bold"/>
                    </Button>
                    <Button Style="{StaticResource ActionButtonStyle}"
                            MinWidth="150"
                            MinHeight="60"
                            Padding="20,12"
                            Click="OnPrintClick">
                        <TextBlock Text="印刷" FontSize="24" FontWeight="Bold"/>
                    </Button>
                    <Button Style="{StaticResource ActionButtonStyle}"
                            Background="#6366F1"
                            MinWidth="150"
                            MinHeight="60"
                            Padding="20,12"
                            Click="OnHelpClick">
                        <TextBlock Text="ヘルプ" FontSize="24" FontWeight="Bold"/>
                    </Button>
                    <Button x:Name="BtnToggleKeypad"
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#8B5CF6"
                            MinWidth="150"
                            MinHeight="60"
                            Padding="20,12"
                            Click="OnToggleKeypadClick"
                            ToolTip="テンキーを表示/非表示">
                        <TextBlock Text="テンキー" FontSize="24" FontWeight="Bold"/>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="1"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="10,0">
                    <TextBlock Text="例: 「A2に1万円入れて」"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ========================================== -->
        <!-- 計算ボタンバー -->
        <!-- ========================================== -->
        <Border Grid.Row="1"
                Background="{StaticResource SurfaceBrush}"
                Padding="8,6"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <WrapPanel Orientation="Horizontal">
                <Button Style="{StaticResource ActionButtonStyle}"
                        Background="#10B981"
                        MinWidth="200"
                        MinHeight="60"
                        Padding="20,12"
                        Click="OnSumClick"
                        ToolTip="選択範囲の合計を計算">
                    <TextBlock Text="合計" FontSize="24" FontWeight="Bold"/>
                </Button>
                <Button Style="{StaticResource ActionButtonStyle}"
                        Background="#8B5CF6"
                        MinWidth="200"
                        MinHeight="60"
                        Padding="20,12"
                        Click="OnAverageClick"
                        ToolTip="選択範囲の平均を計算">
                    <TextBlock Text="平均" FontSize="24" FontWeight="Bold"/>
                </Button>
            </WrapPanel>
        </Border>

        <!-- ========================================== -->
        <!-- コマンド入力: 自然言語で操作 -->
        <!-- ========================================== -->
        <Border Grid.Row="2"
                Background="#EFF6FF"
                Padding="10,8"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="CommandInput"
                         Grid.Column="0"
                         Style="{StaticResource LargeTextBoxStyle}"
                         FontSize="28"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         MinHeight="80"
                         MaxHeight="150"
                         VerticalScrollBarVisibility="Auto"
                         KeyDown="OnCommandKeyDown"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                    <Button MinWidth="180"
                            MinHeight="70"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="OnExecuteCommand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="12"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#667eea" Offset="0"/>
                                            <GradientStop Color="#764ba2" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#667eea" BlurRadius="15" ShadowDepth="3" Opacity="0.4"/>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect Color="#667eea" BlurRadius="20" ShadowDepth="5" Opacity="0.6"/>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="🤖 AI実行" FontSize="28" FontWeight="Bold" Foreground="White"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ========================================== -->
        <!-- スプレッドシート本体とテンキー -->
        <!-- ========================================== -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- スプレッドシート本体 -->
            <syncfusion:SfSpreadsheet x:Name="Spreadsheet"
                                      Grid.Column="0"
                                      FontSize="24"
                                      Margin="10"/>

            <!-- テンキーと計算機能 -->
<Border x:Name="KeypadPanel"
                    Grid.Column="1"
                    Width="260"
                    Background="#F3F0EB"
                    BorderBrush="#E7E2DA"
                    BorderThickness="1,0,0,0"
                    Padding="10"
                    Visibility="Collapsed">
                <StackPanel>
                    <!-- モード切り替えボタン -->
                    <Border Background="#FFFFFF"
                            BorderBrush="#E7E2DA"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="4"
                            Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="BtnCalcMode"
                                    Grid.Column="0"
                                    Content="計算機"
                                    FontSize="14"
                                    FontWeight="Bold"
                                    Padding="8"
                                    Margin="2"
                                    Background="#B8942F"
                                    Foreground="White"
                                    BorderThickness="0"
                                    Click="OnCalcModeClick"
                                    ToolTip="電卓として計算"/>
                            <Button x:Name="BtnInputMode"
                                    Grid.Column="1"
                                    Content="Excel入力"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Padding="8"
                                    Margin="2"
                                    Background="#E7E2DA"
                                    Foreground="#1C1917"
                                    BorderThickness="0"
                                    Click="OnInputModeClick"
                                    ToolTip="セルに式を入力"/>
                        </Grid>
                    </Border>

                    <!-- 計算機モード表示エリア -->
                    <StackPanel x:Name="CalcModePanel" Visibility="Visible">
                        <!-- 計算結果表示 -->
                        <Border Background="#FFFFFF"
                                BorderBrush="#E7E2DA"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8"
                                Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="計算結果"
                                           FontSize="12"
                                           Foreground="#57534E"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBox x:Name="CalcDisplay"
                                         FontSize="24"
                                         FontWeight="Bold"
                                         Foreground="#1C1917"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         IsReadOnly="True"
                                         TextAlignment="Right"
                                         Text="0"
                                         Padding="4"/>
                                <Button Content="結果をセルにコピー"
                                        FontSize="12"
                                        Padding="6,4"
                                        Margin="0,4,0,0"
                                        Background="#B8942F"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Click="OnCopyResultToCell"/>
                            </StackPanel>
                        </Border>

                        <!-- 計算履歴 -->
                        <Border Background="#FFFFFF"
                                BorderBrush="#E7E2DA"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8"
                                Margin="0,0,0,10">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0"
                                               Text="📝 計算履歴"
                                               FontSize="12"
                                               FontWeight="Bold"
                                               Foreground="#57534E"/>
                                    <Button Grid.Column="1"
                                            Content="クリア"
                                            FontSize="10"
                                            Padding="4,2"
                                            Background="#DC2626"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Click="OnClearHistoryClick"
                                            ToolTip="履歴をクリア"/>
                                </Grid>
                                <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                    <ListBox x:Name="CalcHistory"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             SelectionMode="Single"
                                             MouseDoubleClick="OnHistoryItemDoubleClick">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}"
                                                           FontSize="11"
                                                           Foreground="#1C1917"
                                                           TextWrapping="Wrap"
                                                           Margin="2"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </ScrollViewer>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- 入力モード表示エリア -->
                    <StackPanel x:Name="InputModePanel" Visibility="Collapsed">
                        <Border Background="#FFFFFF"
                                BorderBrush="#E7E2DA"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="8"
                                Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock Text="計算式"
                                           FontSize="12"
                                           Foreground="#57534E"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,4"/>
                                <TextBox x:Name="FormulaDisplay"
                                         FontSize="18"
                                         FontWeight="Bold"
                                         Foreground="#1C1917"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         IsReadOnly="False"
                                         TextAlignment="Left"
                                         Text=""
                                         Padding="4"
                                         TextWrapping="Wrap"
                                         MinHeight="50"
                                         TextChanged="OnFormulaDisplayTextChanged"
                                         ToolTip="直接編集できます"/>
                                <Grid Margin="0,4,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button x:Name="BtnCellSelect"
                                            Grid.Column="0"
                                            Content="📍 セル選択"
                                            FontSize="12"
                                            Padding="6,4"
                                            Margin="0,0,2,0"
                                            Background="#2563EB"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Click="OnCellSelectClick"
                                            ToolTip="セルをクリックして式に追加"/>
                                    <Button Grid.Column="1"
                                            Content="← 削除"
                                            FontSize="12"
                                            Padding="6,4"
                                            Margin="2,0,0,0"
                                            Background="#DC2626"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Click="OnBackspaceClick"
                                            ToolTip="最後の文字を削除"/>
                                </Grid>
                                <Button Content="✓ 式を確定"
                                        FontSize="12"
                                        Padding="6,4"
                                        Margin="0,4,0,0"
                                        Background="#16A34A"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Click="OnConfirmFormulaClick"
                                        ToolTip="アクティブセルに式を入力"/>
                            </StackPanel>
                        </Border>

                        <!-- セル選択待機インジケーター -->
                        <Border x:Name="CellSelectIndicator"
                                Background="#DBEAFE"
                                BorderBrush="#2563EB"
                                BorderThickness="2"
                                CornerRadius="4"
                                Padding="8"
                                Margin="0,0,0,10"
                                Visibility="Collapsed">
                            <TextBlock Text="📍 セルをクリックしてください"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="#2563EB"
                                       HorizontalAlignment="Center"
                                       TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>

                    <!-- 数字ボタン（7-9） -->
                    <UniformGrid Rows="1" Columns="3" Margin="0,0,0,5">
                        <Button Content="7" Click="OnNumericKeyClick" Tag="7" FontSize="20" Padding="10" Margin="2"/>
                        <Button Content="8" Click="OnNumericKeyClick" Tag="8" FontSize="20" Padding="10" Margin="2"/>
                        <Button Content="9" Click="OnNumericKeyClick" Tag="9" FontSize="20" Padding="10" Margin="2"/>
                    </UniformGrid>

                    <!-- 数字ボタン（4-6） -->
                    <UniformGrid Rows="1" Columns="3" Margin="0,0,0,5">
                        <Button Content="4" Click="OnNumericKeyClick" Tag="4" FontSize="20" Padding="10" Margin="2"/>
                        <Button Content="5" Click="OnNumericKeyClick" Tag="5" FontSize="20" Padding="10" Margin="2"/>
                        <Button Content="6" Click="OnNumericKeyClick" Tag="6" FontSize="20" Padding="10" Margin="2"/>
                    </UniformGrid>

                    <!-- 数字ボタン（1-3） -->
                    <UniformGrid Rows="1" Columns="3" Margin="0,0,0,5">
                        <Button Content="1" Click="OnNumericKeyClick" Tag="1" FontSize="20" Padding="10" Margin="2"/>
                        <Button Content="2" Click="OnNumericKeyClick" Tag="2" FontSize="20" Padding="10" Margin="2"/>
                        <Button Content="3" Click="OnNumericKeyClick" Tag="3" FontSize="20" Padding="10" Margin="2"/>
                    </UniformGrid>

                    <!-- 数字ボタン（0） -->
                    <UniformGrid Rows="1" Columns="3" Margin="0,0,0,5">
                        <Button Content="0" Click="OnNumericKeyClick" Tag="0" FontSize="20" Padding="10" Margin="2" Grid.ColumnSpan="2"/>
                        <Button Content="." Click="OnNumericKeyClick" Tag="." FontSize="20" Padding="10" Margin="2"/>
                        <Button Content="00" Click="OnNumericKeyClick" Tag="00" FontSize="20" Padding="10" Margin="2"/>
                    </UniformGrid>

                    <!-- 区切り線 -->
                    <Separator Margin="0,10"/>

                    <UniformGrid Rows="2" Columns="2" Margin="0,5,0,10">
                        <Button Content="+" Click="OnOperatorClick" Tag="+" FontSize="28" FontWeight="Bold" Padding="12" Margin="2" Background="#2563EB" Foreground="White" BorderThickness="0" ToolTip="足し算"/>
                        <Button Content="-" Click="OnOperatorClick" Tag="-" FontSize="28" FontWeight="Bold" Padding="12" Margin="2" Background="#2563EB" Foreground="White" BorderThickness="0" ToolTip="引き算"/>
                        <Button Content="×" Click="OnOperatorClick" Tag="*" FontSize="28" FontWeight="Bold" Padding="12" Margin="2" Background="#2563EB" Foreground="White" BorderThickness="0" ToolTip="掛け算"/>
                        <Button Content="÷" Click="OnOperatorClick" Tag="/" FontSize="28" FontWeight="Bold" Padding="12" Margin="2" Background="#2563EB" Foreground="White" BorderThickness="0" ToolTip="割り算"/>
                    </UniformGrid>

                    <!-- 計算機モードのボタン -->
                    <StackPanel x:Name="CalcModeButtons" Visibility="Visible">
                        <Button Content="=" Click="OnEqualsClick" FontSize="28" FontWeight="Bold" Padding="12" Margin="2,0,2,5" Background="#16A34A" Foreground="White" BorderThickness="0" ToolTip="計算実行"/>
                    </StackPanel>

                    <!-- 入力モードのボタン -->
                    <StackPanel x:Name="InputModeButtons" Visibility="Collapsed">
                        <Button Content="=" Click="OnStartFormulaClick" FontSize="28" FontWeight="Bold" Padding="12" Margin="2,0,2,5" Background="#CA8A04" Foreground="White" BorderThickness="0" ToolTip="式を開始（=を追加）"/>
                    </StackPanel>

                    <!-- クリアボタン（共通） -->
                    <Button Content="C (クリア)" Click="OnClearClick" FontSize="14" FontWeight="Bold" Padding="10" Margin="2" Background="#DC2626" Foreground="White" BorderThickness="0" ToolTip="すべてクリア"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
