<Window x:Class="HarmonicSheet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:HarmonicSheet.Views"
        Title="HarmonicOffice - かんたんオフィス"
        WindowState="Maximized"
        MinWidth="1200"
        MinHeight="800"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- 左サイドバー（タブ切り替え） -->
            <ColumnDefinition Width="Auto"/>
            <!-- メインコンテンツ -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ========================================== -->
        <!-- 左サイドバー: タブボタン縦並び -->
        <!-- ========================================== -->
        <Border Grid.Column="0"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,2,0"
                Width="100">
            <StackPanel>
                <!-- ロゴ -->
                <Border BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="5">
                    <TextBlock Text="HO"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}"
                               HorizontalAlignment="Center"
                               ToolTip="HarmonicOffice"/>
                </Border>

                <!-- タブボタン（縦並び） -->
                <!-- 1. ホーム（ポータル） -->
                <RadioButton x:Name="TabPortal"
                             Checked="OnTabChanged"
                             GroupName="MainTabs"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="10"
                             Margin="5,10,5,5"
                             Cursor="Hand">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="2"
                                    CornerRadius="12"
                                    Padding="{TemplateBinding Padding}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock x:Name="Icon"
                                               Text="📋"
                                               FontSize="32"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="Label"
                                               Text="ホーム"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center"
                                               Margin="0,6,0,0"
                                               Foreground="{StaticResource TextBrush}"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Border" Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#667eea" Offset="0"/>
                                                <GradientStop Color="#764ba2" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter TargetName="Label" Property="Foreground" Value="White"/>
                                    <Setter TargetName="Border" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#667eea" BlurRadius="12" ShadowDepth="0" Opacity="0.5"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>

                <!-- 2. 表（Excel） -->
                <RadioButton x:Name="TabSpreadsheet"
                             IsChecked="True"
                             Checked="OnTabChanged"
                             GroupName="MainTabs"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="10"
                             Margin="5"
                             Cursor="Hand">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="2"
                                    CornerRadius="12"
                                    Padding="{TemplateBinding Padding}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock x:Name="Icon"
                                               Text="📊"
                                               FontSize="32"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="Label"
                                               Text="Excel"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center"
                                               Margin="0,6,0,0"
                                               Foreground="{StaticResource TextBrush}"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Border" Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#11998e" Offset="0"/>
                                                <GradientStop Color="#38ef7d" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter TargetName="Label" Property="Foreground" Value="White"/>
                                    <Setter TargetName="Border" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#11998e" BlurRadius="12" ShadowDepth="0" Opacity="0.5"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>

                <!-- 3. 文書 -->
                <RadioButton x:Name="TabDocument"
                             Checked="OnTabChanged"
                             GroupName="MainTabs"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="10"
                             Margin="5"
                             Cursor="Hand">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="2"
                                    CornerRadius="12"
                                    Padding="{TemplateBinding Padding}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock x:Name="Icon"
                                               Text="📄"
                                               FontSize="32"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="Label"
                                               Text="文書"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center"
                                               Margin="0,6,0,0"
                                               Foreground="{StaticResource TextBrush}"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Border" Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#4facfe" Offset="0"/>
                                                <GradientStop Color="#00f2fe" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter TargetName="Label" Property="Foreground" Value="White"/>
                                    <Setter TargetName="Border" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#4facfe" BlurRadius="12" ShadowDepth="0" Opacity="0.5"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>

                <!-- 4. メール -->
                <RadioButton x:Name="TabMail"
                             Checked="OnTabChanged"
                             GroupName="MainTabs"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="10"
                             Margin="5"
                             Cursor="Hand">
                    <RadioButton.Template>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="Transparent"
                                    BorderThickness="2"
                                    CornerRadius="12"
                                    Padding="{TemplateBinding Padding}">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock x:Name="Icon"
                                               Text="📧"
                                               FontSize="32"
                                               HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="Label"
                                               Text="メール"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               HorizontalAlignment="Center"
                                               Margin="0,6,0,0"
                                               Foreground="{StaticResource TextBrush}"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Border" Property="Background">
                                        <Setter.Value>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#fa709a" Offset="0"/>
                                                <GradientStop Color="#fee140" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter TargetName="Label" Property="Foreground" Value="White"/>
                                    <Setter TargetName="Border" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Color="#fa709a" BlurRadius="12" ShadowDepth="0" Opacity="0.5"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Border" Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </RadioButton.Template>
                </RadioButton>

                <!-- 困ったときボタン -->
                <Button Margin="5,20,5,5"
                        Padding="12"
                        Cursor="Hand"
                        Click="OnHelpLineClick"
                        ToolTip="電話サポート・困ったとき">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#DC2626" Offset="0"/>
                                        <GradientStop Color="#EF4444" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="12"
                                                Padding="{TemplateBinding Padding}">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#DC2626" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/>
                                            </Border.Effect>
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🆘"
                                   FontSize="28"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="困った"
                                   FontSize="12"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                </Button>

                <!-- 設定ボタン -->
                <Button Content="&#xE713;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="24"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{StaticResource TextSecondaryBrush}"
                        Padding="10"
                        Margin="5,10,5,5"
                        Cursor="Hand"
                        HorizontalAlignment="Center"
                        ToolTip="設定"
                        Click="OnSettingsClick"/>
            </StackPanel>
        </Border>

        <!-- ========================================== -->
        <!-- メインコンテンツ -->
        <!-- ========================================== -->
        <Grid Grid.Column="1">
            <!-- ポータルビュー -->
            <views:PortalView x:Name="PortalView"
                              Visibility="Collapsed"/>
            <!-- 表計算ビュー -->
            <views:SpreadsheetView x:Name="SpreadsheetView"
                                   Visibility="Visible"/>
            <!-- 文書ビュー -->
            <views:DocumentView x:Name="DocumentView"
                                Visibility="Collapsed"/>
            <!-- メールビュー -->
            <views:MailView x:Name="MailView"
                            Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
